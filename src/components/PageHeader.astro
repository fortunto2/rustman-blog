---
interface Props { title: string; description?: string; }
const { title, description } = Astro.props;
---
<div class="mb-8">
  <h1 class="text-2xl font-bold mb-2 font-mono prompt">{title}</h1>
  {description && <p class="text-[var(--color-muted)]">{description}</p>}
</div>
